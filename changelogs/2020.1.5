## 2020.1.5 Changelog
### Changes from last patch:
#### Additions:

 - A new image_filter, rotate_image, a one-fit-all functions for rotating images 

#### Bug Fixes:

 - Added a missing import for NetworkTablesConnection.get_table()
 - Fixed default values for `erosion` and `dilation` morphological functions
 - Corrected typo in `erosion` and `dilation`, `border_values` -> `border_value`